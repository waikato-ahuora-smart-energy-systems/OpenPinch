# HENS-11 Adversarial Review

## Findings

No findings. HENS-11 is cleared for this review pass.

## Scope And Contract Checks

- The reviewed HENS-11 slice is limited to regression and retirement-gate coverage: one HENS-11 task-note update, one new HENS-11 test module, and one Nine-stream best-ESM network snapshot. The worktree does not show OpenHENS repository/package metadata changes, runtime compatibility shims, import aliases, field aliases, wrapper-package work, solver algorithm changes, distributed execution, or multi-utility expansion in the HENS-11 files (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:53-93`, `tests/test_heat_exchanger_network_synthesis_hens11.py:1-673`, `openhens_baseline_results/network_snapshots/Nine-stream-Linnhoff-and-Ahmad-1999-1/best-esm.json:1-166`).
- Four-stream remains the routine baseline and Nine-stream remains gated as final verification. The task keeps Four-stream routine coverage checked, leaves the live Nine-stream final-verification solver checkbox unchecked, and documents that Couenne/IPOPT are missing in this environment (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:53-55`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:107-113`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:174-178`). The Nine-stream artifact/snapshot checks are fast evidence over checked-in artifacts, not a claimed live solver pass.
- Solver scaffolding is marked and separated from default fast tests. The only live OpenPinch solve test in the new module is decorated with `@pytest.mark.solver`, calls the OpenPinch `PinchProblem` plus `heat_exchanger_network_synthesis_service(..., executor=LocalSynthesisExecutor(...))` path, and checks for `couenne` and `ipopt` before running (`tests/test_heat_exchanger_network_synthesis_hens11.py:266-301`, `tests/test_heat_exchanger_network_synthesis_hens11.py:576-590`). Missing binaries are not claimed as solver success: the task records `2 skipped, 6 deselected`, names the missing binaries, gives the rerun command, and leaves live solver verification checkboxes unchecked (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:174-178`).
- Artifact-backed metric coverage is broad enough for the checked boxes. The test compares best ESM TAC, quartiles, within-2/5/10 percent counts, attempted/solved counts, solved ESM counts, stage/unit counts, `dTmin`, `min_dQ`, utility loads, solver status, and verification failures for Four-stream and Nine-stream artifacts (`tests/test_heat_exchanger_network_synthesis_hens11.py:44-98`, `tests/test_heat_exchanger_network_synthesis_hens11.py:101-154`). The checked-in summaries contain the expected metric fields for both cases, and the solution-metrics CSV rows include the selected ESM task IDs, solver status, TAC, stage/unit counts, and empty verification failures.
- Network snapshot comparisons use stable stream identities and labelled network content rather than raw positional arrays. The Nine-stream snapshot includes source artifact path, summary artifact path, task id, method, best TAC, `dTmin`, derivative threshold, stage count, recovery/HU/CU/total unit counts, utility loads, tolerances, and each recovery/utility exchanger's kind, source stream, sink stream, stage, duty, and area (`openhens_baseline_results/network_snapshots/Nine-stream-Linnhoff-and-Ahmad-1999-1/best-esm.json:1-166`). The test reconstructs an identity-labelled `HeatExchangerNetwork`, then looks up each expected exchanger by source/sink/stage/kind and compares duty and area (`tests/test_heat_exchanger_network_synthesis_hens11.py:157-228`, `tests/test_heat_exchanger_network_synthesis_hens11.py:304-420`).
- Numerical invariants are meaningful and independent of workbook regeneration. They operate on checked-in best-ESM artifacts plus OpenPinch-compatible JSON fixtures, not workbook files, and cover process-stream heat balances, per-stage recovery duty balance, recovery approach-temperature feasibility, non-negative areas, active temperature ordering, utility-cost recomputation, and area/fixed-cost recomputation (`tests/test_heat_exchanger_network_synthesis_hens11.py:231-263`, `tests/test_heat_exchanger_network_synthesis_hens11.py:460-573`).
- Additional small/medium/large tier cases are not overclaimed. The expanded-tier solver checkbox remains unchecked, and the implementation note explicitly says no additional tier cases were added because exact source paths, hashes, migrated fixture paths, grids, and expected artifact paths were not named beyond Four-stream and Nine-stream (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:56-57`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:185-189`).
- Temporary shadow tests are not removed prematurely. The task keeps the transition-shadow checkbox checked as still justified, but leaves the removal checkbox unchecked until OpenPinch-owned baselines fully cover migrated behavior (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:81-84`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:123-124`).
- Retirement and deprecation remain gated. Archive/freeze, OpenHENS README/package deprecation, OpenHENS deprecation-note verification, one-maintained-implementation, and maintained-implementation DoD checkboxes remain unchecked, and the notes say this environment has not proven live Four/Nine solver parity or confirmed an available OpenPinch release containing the replacement synthesis API (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:87-90`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:102-116`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:125-128`, `docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:190-194`).
- The task notes are library-quality for this stage: they describe stable regression and retirement contracts, keep migration constraints explicit, identify the missing solver blocker and rerun command, and avoid promising compatibility aliases or actual OpenHENS retirement before release readiness (`docs/developer/openhens-integration-tasks/11-regression-expansion-and-openhens-retirement.md:139-194`).

## Residual Risks

- I did not rerun pytest, ruff, docs, or solver commands because this review is read-only except for this review artifact; those commands can write caches or generated files. I relied on direct inspection plus the task's recorded command evidence.
- Live Four-stream and Nine-stream OpenPinch solver parity is still unproven in this environment because `couenne` and `ipopt` are missing. The task correctly leaves the solver and retirement gates unchecked and documents the rerun command.
- The fast artifact/snapshot tests validate checked-in source artifacts and OpenPinch extraction/invariant behavior. They do not prove the moved OpenPinch solver can regenerate the same artifacts until the marked solver tests run on a solver-capable machine.
- `.DS_Store` is still modified in the worktree. It is outside HENS-11 scope and should remain excluded from staging/publication.

## Verdict

HENS-11 is cleared for this review pass. The remaining solver parity and retirement items are correctly documented as gated residual risks, not review-blocking findings.
