# OpenPinch/pytest.ini

[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore:Import from 'OpenPinch' instead of the repository root package\.:DeprecationWarning
    ignore::DeprecationWarning:pydantic.*
