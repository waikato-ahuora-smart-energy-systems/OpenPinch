# Read the Docs Configuration Clarification

The repository already contains a valid root `.readthedocs.yaml`. Please select
the outcome you want so the requirements can distinguish configuration work
from hosted-build troubleshooting.

## Question 1
What should be done with the existing Read the Docs configuration?

A) Confirm that the existing configuration is correct and make no repository
changes (recommended)

B) Troubleshoot a failing Read the Docs hosted build using its build log

C) Change the existing configuration; describe the desired change after the
`[Answer]:` tag

X) Other (please describe after the `[Answer]:` tag below)

[Answer]: B
