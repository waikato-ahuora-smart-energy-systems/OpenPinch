# Cleanup unit and boundary verification

Run the combined focused command in integration-test-instructions.md. It includes architecture, resources, and repository entrypoints. No new tests are necessary for deleting unconsumed artifacts. Ruff checks for scripts, examples, and the Streamlit entrypoint also passed using `--no-cache`.
