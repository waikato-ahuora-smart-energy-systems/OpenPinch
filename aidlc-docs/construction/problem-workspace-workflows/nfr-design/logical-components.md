# Logical Components

The unit uses the existing problem component/design/plot descriptors, one HEN
design view, the workspace case store, and an ordered case-batch view. Internal
numerical services remain unchanged owners. No queue, cache server, database,
circuit breaker, deployment resource, or new network integration is required.
