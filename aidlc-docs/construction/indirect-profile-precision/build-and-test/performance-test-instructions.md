# Performance Test Instructions

No dedicated performance benchmark is required. The fix adds two bounded
Problem Table copies during direct graph extraction but does not change solver
complexity, iteration counts, dependencies, or deployment behavior. The
complete non-solver suite duration provides a practical regression observation.
