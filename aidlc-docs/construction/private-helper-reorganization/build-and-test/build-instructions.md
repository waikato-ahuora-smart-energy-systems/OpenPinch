# Build Instructions

From the repository root, build isolated distribution artifacts with:

```text
uv run python scripts/build_dist.py --output-dir /tmp/openpinch-dist
```

Expected artifacts are the `openpinch-0.4.6` wheel and source distribution.

