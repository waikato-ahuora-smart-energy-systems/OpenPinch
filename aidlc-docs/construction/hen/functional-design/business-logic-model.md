# Business Logic Model

Prepared arrays retain parent axes and add profile tensors. Stage boundaries use cumulative heat coordinates linked to temperature through ordered piecewise relations. Each parent match obtains a monotone path through hot/cold segment pairs; its local duties sum to the parent match duty and its local areas sum to the exchanger area.
