# Tutorial NFR Requirements

All notebooks must parse, compile, have empty outputs/counts, avoid unused or
private imports, and use the final root workflow. Base tutorials must execute in
a clean kernel; slow-HPR and solver notebooks execute only in their declared
optional profiles. Every supported operation must appear in the canonical CSV
and at least one notebook. Security, availability, and infrastructure are N/A.
